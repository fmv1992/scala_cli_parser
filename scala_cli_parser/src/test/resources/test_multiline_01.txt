# Has the --execute flag as default.
# Default can be 'yes', 'true' or '1'.

name: multiline
    n: 1
    type: int
    help:   | This is a multi line help string.
            |
            | It may also contain examples and etc...
    other:  | This just contains a perchance aligned '|' on this line. It is a single line.
    default: yes

# vim: set filetype=config fileformat=unix wrap:
